import React from "react";
import ListingPage from "./ListingPage";

const MainSection = () => {
  return (
    <div className="mt-2 justify-center items-center gap-2">
      <ListingPage />
    </div>
  );
};

export default MainSection;
